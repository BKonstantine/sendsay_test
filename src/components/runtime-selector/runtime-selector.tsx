import { FC } from "react";
import style from "./runtime-selector.module.css";

const RuntimeSelector: FC = () => {
  return <div className={style.selector}></div>;
};

export default RuntimeSelector;
